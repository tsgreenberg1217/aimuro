query Cards($name: String!) {
    cards(filter: { name: $name }) {
        name
        rarity
        code
        level
        color
        cardType
        zone
        trait
        link
        ap
        hp
        sourceTitle
    }
}
