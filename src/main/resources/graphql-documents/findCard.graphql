query FindCard($name: String!) {
    cards: getCard(name: $name) {
        id
        code
        rarity
        name
        effect
        level
        cost
        color
        cardType
        zone
        trait
        link
        ap
        hp
        sourceTitle
        getIt
    }
}
