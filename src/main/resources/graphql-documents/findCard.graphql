query Cards($name: String!) {
    cards(filter: { name: $name }) {
        name
        rarity
        code
        level
        cost
        color
        effect
        cardType
        zone
        trait
        link
        ap
        hp
        sourceTitle
    }
}
