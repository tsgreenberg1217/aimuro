query Cards($filter: CardFilter) {
    cards(filter: $filter) {
        name
        rarity
        code
        level
        cost
        color
        effect
        cardType
        zone
        trait
        link
        ap
        hp
        sourceTitle
    }
}
