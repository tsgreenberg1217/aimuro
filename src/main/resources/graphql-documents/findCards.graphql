query Cards($filter: CardFilter) {
    cards(filter: $filter) {
        name
        rarity
        code
        level
        color
        cardType
        zone
        trait
        link
        ap
        hp
        sourceTitle
    }
}
