spring:
  datasource:
    url: jdbc:postgresql://pgvector:5432/gundam-tcg-rules
    username: postgres
    password: password
  data:
    redis:
      host: localhost
      port: 6379
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration
      - org.springframework.boot.autoconfigure.data.jpa.JpaRepositoriesAutoConfiguration

app:
  conversation:
    datasource:
      url: jdbc:postgresql://postgres:5433/aimuro-app
      username: postgres
      password: password
