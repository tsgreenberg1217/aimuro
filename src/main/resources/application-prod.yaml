spring:
  datasource:
    url: jdbc:postgresql://pgvector:5432/gundam-tcg-rules
    username: postgres
    password: password
  data:
    redis:
      host: localhost
      port: 6379
app:
  conversation:
    datasource:
      url: jdbc:postgresql://postgres:5432/aimuro-app
      username: postgres
      password: password
