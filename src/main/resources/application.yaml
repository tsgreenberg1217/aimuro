gundam:
  card:
    service:
      url: http://localhost:8082/graphql

spring:
  profiles:
    active: debug
  threads:
    virtual:
      enabled: true
  application:
    name: aimuro
  ai:
    vectorstore:
      pgvector:
        initialize-schema: true
    openai:
      api-key: ${OPEN_AI_KEY}
      model: o4-mini
